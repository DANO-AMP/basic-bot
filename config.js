let config = {
    token: "null", // Reemplaze esto con el token de tu de bot.
    prefix: "!", // El prefix de su bot para indentificar sus comandos.
  
  }
  
  // Aqui exportamos el objeto config:
  module.exports = config;